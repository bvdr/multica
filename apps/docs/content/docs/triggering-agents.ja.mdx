---
title: エージェントに仕事を任せる
description: エージェントをトリガーする 4 つの方法と、トリガー後に進捗を確認する場所。
---

Multica では、エージェントが自分から作業を始めることはありません。すべての実行は、明示的なアクションによってトリガーされます。方法は 4 つあります。

| 方法 | 適した場面 | 操作 |
| --- | --- | --- |
| **Issue の割り当て** | この作業をエージェントに最初から最後まで任せたい | [issue をエージェントに割り当てます](/assigning-issues)。issue が `todo` 以降のステータスにあれば即座に開始します |
| **コメントでの @メンション** | issue の担当者を変えずに、具体的な依頼を 1 件処理してもらいたい | [コメントで @メンションします](/mentioning-agents)。エージェントのコメントに返信して会話を続けることもできます |
| **直接チャット** | issue を作るまでもない質問や試行 | [エージェントとチャットを開始します](/chat)。メッセージ 1 件ごとに 1 回の実行がトリガーされます |
| **オートパイロット** | 時刻や外部イベントに応じて自動で進めたい繰り返し作業 | [オートパイロットを作成します](/autopilots)。スケジュール、webhook、または手動でトリガーされます |

4 つの方法には共通のルールがあります。

- トリガーできるのは、自分に**実行権限がある**エージェントだけです。これはエージェントの Access 設定で決まり、ワークスペースのロールとは無関係です。
- Issue からトリガーする前に、入力欄の下に**トリガープレビュー**が表示され、この内容が誰を起こすかが明示されます。どのエージェントもトリガーしたくないときは、`/note` でメモを残してください。
- `backlog` にある issue は実行をトリガーしません。issue を `todo` に移すと、割り当て済みのエージェントが開始します。

## 進捗の確認

トリガーのたびに 1 件の[タスク](/tasks)が作成されます。進捗は、トリガーした場所 — issue のタイムラインまたはチャットウィンドウ — にリアルタイムで書き戻されます。実行ログでは、実行の全過程の確認、停止、リトライができます。

実行がなかなか始まらない場合は、ランタイムがオフラインであることがほとんどです。[トラブルシューティング](/troubleshooting)の issue が実行を開始しない場合のセクションに沿って確認してください。
